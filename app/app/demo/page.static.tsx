import Console from "@/components/Console";

/** Static demo export: the console on recorded engine output with pre-rendered Rime audio. */
export default function StaticDemoPage() {
  return <Console demo />;
}
