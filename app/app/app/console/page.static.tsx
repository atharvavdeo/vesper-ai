import Console from "@/components/Console";

// Static demo export (STATIC_DEMO=1): the phone field console on recorded engine output.
export default function StaticFieldConsolePage() {
  return <Console demo />;
}
