import Console from "@/components/Console";

// The original phone-first field console (Live · Talk · Logs · Scenarios · Enroll + its guided tour).
export default function FieldConsolePage() {
  return <Console />;
}
