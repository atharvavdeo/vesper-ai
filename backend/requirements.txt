fastapi
uvicorn[standard]
python-multipart
httpx
python-dotenv
openai
livekit-api
PyJWT[crypto]
resend>=2.45
# --- W1 memory layer ---
lancedb==0.38.0
pyarrow==25.0.1
numpy==2.5.3
sentence-transformers==6.0.1   # bge-reranker-v2-m3 cross-encoder (torch/MPS)
torch==2.14.0
pymupdf==1.28.2
python-docx==1.2.0
openpyxl==3.1.5
pandas==3.0.5
beautifulsoup4==4.15.0
lxml==6.1.3
# Embeddings: Ollama bge-m3 over HTTP (httpx above). `ollama pull bge-m3`
# cognee==1.5.4 is installed in a SEPARATE venv (backend/.venv-cognee) because it pins openai<3 and
# websockets<16, which would downgrade this venv. Setup:
#   python3.13 -m venv backend/.venv-cognee && backend/.venv-cognee/bin/pip install cognee==1.5.4 python-dotenv httpx
